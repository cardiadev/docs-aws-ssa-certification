# Copy this file to scripts/.env and fill in your values
# scripts/.env is git-ignored — never commit real tokens

# Required
KK_TOKEN="eyJhbGci"
KK_API_URL="https://"
KK_SITE_URL="https://"

# Optional overrides
# COURSE_SLUG=aws-solutions-architect-associate-certification
# OUTPUT_DIR=scripts/output
# OUTPUT_FORMAT=both   # json | md | both
# DRY_RUN=1            # set to 1 to list lessons without downloading
